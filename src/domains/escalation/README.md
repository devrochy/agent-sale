# domains/escalation

Máquina de estados de escalamiento a humano y `handoff_queue`: tool `escalar_a_humano` (ver `docs/fase-7-escalamiento-humano/`).

Se implementa en el incremento de Fase 7 (código). No se importa directamente desde otros `domains/*` — solo a través de `orchestrator`.
