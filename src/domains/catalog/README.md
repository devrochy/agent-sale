# domains/catalog

Catálogo e inventario: tool `consultar_inventario`, sincronización con la fuente real (hoy Google Sheets — ver `docs/fase-5-catalogo-inventario/`), caché en Redis.

Se implementa en el incremento de Fase 5 (código). No se importa directamente desde otros `domains/*` — solo a través de `orchestrator`.
