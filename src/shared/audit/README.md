# shared/audit

Logging de auditoría (`audit_log`) de cada tool ejecutada: input/output reales, no lo que el LLM propuso (ver `docs/fase-4-motor-agente/auditoria.md`).

Se implementa junto con `orchestrator` (Fase 4, código).
