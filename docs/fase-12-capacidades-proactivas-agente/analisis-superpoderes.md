# Análisis de diseño: los 12 "superpoderes" de Forja+ aplicados a agent-sale

Fuente: sección `/admin/upgrade` ("Los 12 superpoderes de Forja+") del panel de referencia analizado en la [Fase 11](../fase-11-panel-admin-dashboard/README.md). En el producto de referencia son funcionalidades de pago (PRO); en agent-sale **no se adopta ese modelo de negocio** — se evalúa cada una únicamente por si le agrega valor real al asistente de ventas y qué tan factible es construirla sobre la arquitectura actual, sin gating de ningún tipo.

A diferencia del panel admin (Fase 11, que en su mayoría muestra datos que el sistema ya genera), la mayoría de estas 12 capacidades son **comportamiento nuevo del agente/orquestador** — el panel admin es, cuando aplica, solo el lugar donde se ve el resultado (ej. historial de reportes diarios, incidentes de guardrail). Por eso viven en una fase separada: no son "más pantallas del panel", son capacidad nueva del sistema.

Dos decisiones técnicas transversales que desbloquean 5 de las 12 se resuelven una sola vez: [ADR-018](./adrs/ADR-018-infraestructura-jobs-programados.md) (cómo programar tareas periódicas) y [ADR-019](./adrs/ADR-019-mensajeria-proactiva-ventana-24h.md) (qué se puede mandar proactivamente hoy vs qué requiere aprobación de plantillas de Meta).

| # | Superpoder (referencia) | Qué ya existe en agent-sale | Qué falta construir | Valor/Esfuerzo | Bloqueo externo |
|---|---|---|---|---|---|
| 1 | Blindaje anti-invento | Instrucción de prompt ya cubre precio/stock/promociones (`systemPrompt.ts:11`); verificación determinística de código ya existe **solo para precio** (`priceGuardrail.ts`) | Extender la misma verificación determinística a stock (comparar contra el resultado real de `consultar_inventario`, igual que ya se hace con montos) | Alto/Medio | No |
| 2 | Vigilante (avisa cuando un cliente se enoja o venta en riesgo) | Ya notifica por WhatsApp en cada escalamiento, incluyendo la razón `queja` (`escalarHumano.ts:84-87`, canal genérico `sendMessage.ts`) | Nada nuevo de infraestructura — es en gran parte el mismo mecanismo de handoff ya construido en la Fase 7, solo falta decidir si se quiere una señal adicional de "riesgo" más allá de las 7 razones de escalamiento ya existentes | Alto/Muy bajo | No |
| 3 | Cazador de ventas (reengancha 3-20h después) | **Implementado** (Fase 12.2, `src/jobs/cazadorDeVentas.ts`, node-cron cada hora — [ADR-018](./adrs/ADR-018-infraestructura-jobs-programados.md)). Una sola query trae candidatas (`quotes` sin `orders`, creadas hace 3-20h) *e* incluye el chequeo de ventana de 24h de [ADR-019](./adrs/ADR-019-mensajeria-proactiva-ventana-24h.md) (último mensaje inbound del cliente dentro de 24h) en el mismo `WHERE`. Marca `quotes.follow_up_sent_at` (migración `0025`) solo cuando el envío no lanzó, para poder reintentar en la próxima corrida sin reenganchar dos veces. El mensaje queda persistido en `messages` (`appendMessage`, reusado de `src/orchestrator/memory.ts`) para que el historial de la conversación quede completo | — | Alto/Medio | No, si se dispara dentro de 24h |
| 4 | Handoff que atina (pasa a un humano con contexto) | **Completo** — `escalar_a_humano`, `handoff_queue`, notificación con contexto vía WhatsApp, vista de asesor con historial completo (Fase 7) | Nada | Alto/Ninguno | No |
| 5 | Oído y vista (voz e imágenes entrantes) | Solo saliente: el sistema ya adjunta fotos de producto (`image_url`) en respuestas. Entrante es 100% texto hoy (`webhookHandler.ts:29` solo lee `Body`; `webhook-contrato.md` deja explícito que audio/imagen entrante está fuera del MVP) | Pipeline nuevo completo: descarga de media de Twilio (`MediaUrl0`), transcripción de audio (speech-to-text), y llamada a un modelo con visión para imágenes (comprobantes, fotos de producto que pregunta el cliente) | Alto/Alto | No, pero es la de mayor esfuerzo de las 12 — candidato a su propia fase si se prioriza |
| 6 | Voz de marca (tono editable en vivo) | **Resuelto** — [ADR-021](../fase-11-panel-admin-dashboard/adrs/ADR-021-tono-personalizable-cache-jerarquico.md): 3 variantes fijas de tono (Cálido/Formal/Divertido) como segundo bloque de `system`, con su propio `cache_control` — probado en producción con lecturas de caché completas | Nada | Bajo/Bajo | No |
| 7 | Reporte diario | **Implementado** (Fase 12.2, `src/jobs/dailyReport.ts` + `src/jobs/scheduler.ts`, node-cron `0 8 * * *` en `America/Bogota` — [ADR-018](./adrs/ADR-018-infraestructura-jobs-programados.md)). Consulta propia (mensajes, clientes únicos, conversaciones cerradas/escaladas, pedidos confirmados) acotada al día calendario de ayer en Bogotá — no reusa tal cual las queries de Resumen/funnel (esas son de ventana rodante de 24h/7d, semántica distinta a "ayer"). Destinatario: `tenants.report_recipient_phone` (migración `0024`, configurable desde Configuración en el panel), deliberadamente separado de `human_agents` | — | Alto/Bajo | No |
| 8 | Multi-idioma | Nada explícito — el prompt está en español sin instrucción de idioma ni detección (`systemPrompt.ts:8-48`) | Una instrucción **estática** ("responde en el idioma que use el cliente") agregada al `SYSTEM_PROMPT`. A diferencia de "Voz de marca", esto **no** rompe el prompt caching porque es una regla fija igual para todos los tenants, no un valor que varíe por tenant — no aplica el mismo bloqueo que el punto 6 | Alto/Muy bajo | No |
| 9 | Encuestas de satisfacción | **Implementado** (Fase 12.2, `src/orchestrator/satisfactionSurvey.ts`). Se dispara desde `resolverConversacion` al cerrar. La respuesta se captura como efecto secundario NO bloqueante en `consumer.ts` (antes de `appendInbound`) — nunca intercepta ni descarta el mensaje del cliente, así no se arriesga a tragarse un pedido real si responde "5, y de paso quiero comprar otro casco". `conversations.survey_sent_at`/`survey_reply_processed_at`/`satisfaction_score` (migración `0026`) — un solo intento por conversación cerrada, ventana de 48h. Promedio, distribución 1-5 y reseñas recientes visibles en `/admin/:tenantId/analitica` (extensión post-QA) | — | Medio/Medio | No |
| 10 | Reactivación de leads fríos (días) | Mismo mecanismo técnico que el punto 3, pero el disparo cae **fuera** de la ventana de 24h | Job programado + **plantillas de WhatsApp aprobadas por Meta** (retomar `docs/fase-3-whatsapp-gateway/plantillas-mensajes.md`, nunca implementado) | Alto/Alto | **Sí** — aprobación de Meta, tiempo no controlable (ver [ADR-019](./adrs/ADR-019-mensajeria-proactiva-ventana-24h.md)) |
| 11 | Reseñas (pedir reseña a clientes contentos) | **Implementado y revisado tras QA con el usuario** (mismo módulo que Encuestas + `src/reviews/reviewView.ts`). Diseño original mandaba al cliente directo a `tenants.review_link` (Google) — cambiado a **embudo propio**: el link de WhatsApp abre `/resena/:token` (página propia, texto libre guardado en `reviews`, con RLS), y solo *después* de enviarla se ofrece un botón opcional para compartirla también en la plataforma externa (`GET /resena/:token/compartir`, marca `reviews.shared_publicly`). Así el contenido queda disponible para análisis/estrategia de mercadeo, no solo en Google. Criterio de "cliente contento": calificación de la encuesta ≥ 4 | — | Medio/Bajo | No |
| 12 | Cobros por WhatsApp | **Implementado** (Fase 12.4, ver [ADR-024](./adrs/ADR-024-cobros-wompi-confirmacion-automatica.md)). Nuevo método `pago_en_linea` genera un link de pago de Wompi (tarjeta/PSE/Nequi/Bancolombia Transfer bajo el mismo link); el webhook `transaction.updated` confirma el pago automáticamente (`orders.payment_status`) y notifica al operador para aprobar el envío, sin revisión manual de comprobante | — | Medio/Bajo (revisado de Alto/Alto tras conocer la API real de Wompi) | Cuenta comercial de Wompi para producción (decisión de negocio, no técnica) — se construye y prueba contra sandbox |

## Recomendación de fases dentro de la Fase 12

**12.1 — Quick wins (bajo esfuerzo, sin bloqueo externo):** Multi-idioma (#8), extender guardrail a stock (#1), superficie de "Vigilante" en el panel (#2, ya es casi todo dato existente). Ninguna requiere [ADR-018](./adrs/ADR-018-infraestructura-jobs-programados.md).

**12.2 — Jobs programados + mensajería dentro de 24h: completa.** Reporte diario (#7, **implementado**), Cazador de ventas (#3, **implementado**), Encuestas (#9, **implementado**), Reseñas (#11, **implementado**). Todas dependen de [ADR-018](./adrs/ADR-018-infraestructura-jobs-programados.md) y respetan el límite de [ADR-019](./adrs/ADR-019-mensajeria-proactiva-ventana-24h.md).

**12.3 — Bloqueadas por aprobación externa:** Reactivación de leads fríos (#10) — no se agenda esfuerzo de código hasta iniciar el trámite de plantillas con Meta.

**12.4 — Cobros por WhatsApp: completa.** Cobros (#12, **implementado**, ver [ADR-024](./adrs/ADR-024-cobros-wompi-confirmacion-automatica.md)) — el usuario decidió priorizarla dentro de esta fase en vez de dejarla para una fase propia, una vez conocido el esfuerzo real de la API de Wompi (Medio/Bajo, no Alto/Alto).

**Fuera de la Fase 12, candidata a fase propia:** Oído y vista (#5) — alto esfuerzo y alcance suficiente para su propia fase con su propia ADR (integración de STT/visión), no se diseña en detalle en este documento para no diluir el análisis de lo que sí es factible a corto plazo.

## Qué panel/dato de la Fase 11 se conecta con cada superpoder implementado

- Reporte diario, Cazador de ventas, Encuestas y Reseñas generan eventos que valen la pena mostrar en el panel admin — se agregan como vistas nuevas del [Overview](../fase-11-panel-admin-dashboard/overview-kpis.md) o de [Analítica](../fase-11-panel-admin-dashboard/analitica-costos.md) cuando se implementen, no se diseñan aquí para no anticipar UI de una capacidad que todavía no existe en backend.
- Los incidentes de guardrail (#1) ya se loguean (`orchestrator.guardrail_precio_incidente`) — extenderlos a stock y mostrarlos en el panel es una extensión natural de [Tickets](../fase-11-panel-admin-dashboard/conversaciones-leads-tickets.md#tickets-get-admintenantidtickets) o de Analítica, a definir en implementación.
